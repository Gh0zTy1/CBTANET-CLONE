

/**
 * Representa un area propedeutica registrada en el sistema.
 * Utilizada por materias y grupos para dar identidad a los mismos.
 */
export interface AreaPropedeutica {
  id: number;
  nombre: string;
  descripcion: string
}